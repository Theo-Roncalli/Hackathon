#!/usr/bin/env nextflow

process {
    withName:Download {
        container = 'staphb/sratoolkit' 
    }
     
}

docker {
    enabled = true
    //sudo = true
    //temp = '$PWD' 
    //runOptions = '-d' 
    
    
}
